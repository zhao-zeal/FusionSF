# @package _global_

defaults:
  - fusionsf_pipeline_v1_legacy_matched_zeroshot
  - _self_

experiment_id: ${now:%Y%m%d_%H%M%S}_fusionsf_fixedv1_legacy_matched_zeroshot_vq_paper_best_seed${seed}
task_name: ${experiment_id}

pl_module:
  model:
    vq_in_ts: true
    vq_in_ctx: true
    vq_in_guide: false
