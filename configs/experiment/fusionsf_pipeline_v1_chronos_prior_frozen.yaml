# @package _global_

defaults:
  - fusionsf_pipeline_v1_chronos_prior_baseline
  - _self_

experiment_id: ${now:%Y%m%d_%H%M%S}_fusionsf_fixedv1_chronos_prior_frozen_seed${seed}
task_name: ${experiment_id}

datamodule:
  representation_cache_dir: ${paths.root_dir}/outputs/chronos2_representation_cache/fixedv1_seed42_mean_v1

pl_module:
  model:
    temporal_prior_mode: chronos
